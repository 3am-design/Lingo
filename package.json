{
  "name": "lingo-marketing-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "pages": "node build/render.mjs",
    "pages:watch": "node --watch build/render.mjs",
    "dev": "node build/render.mjs && vite --host 0.0.0.0",
    "build": "node build/render.mjs && vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "devDependencies": {
    "vite": "^7.1.7"
  },
  "dependencies": {
    "lenis": "^1.3.25"
  }
}
